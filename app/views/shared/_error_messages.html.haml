-if object.errors.any?
  #error_explanation
    The form contains
    =pluralize( object.errors.count, "error")
    %ul  
    -object.errors.full_messages.each do |m|
      %li
        *
        =m
    
